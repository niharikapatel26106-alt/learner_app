# Learner App

Flutter mobile application demonstrating signup, login, API integration,
local persistence, detail navigation, settings, profile/favorites and notifications.

## Run

1. Install Flutter.
2. Run `flutter pub get`.
3. Run `flutter run`.

## Important

The API uses JSONPlaceholder for demonstration.
Local storage uses SharedPreferences.
Notification testing uses flutter_local_notifications.
